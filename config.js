// config.js
// Use the local backend by default for development.
// If you want to use ngrok instead, update BACKEND_URL here.
export const BACKEND_URL = "http://127.0.0.1:4040";

// Fallback local IP if needed
export const LOCAL_BACKEND_URL = "http://127.0.0.1:4040";

